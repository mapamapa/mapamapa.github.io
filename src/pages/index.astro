---
import BaseLayout from '@/layouts/BaseLayout.astro';
import site from '../../public/data/site.json';

const { projects, pye, services } = site;
---

<BaseLayout>
  <!-- Hero: Services + Logo + Office + image collage -->
  <section class="hero">
    <div class="hero__logo-wrap reveal reveal-delay-1">
      <p class="hero__services" id="hero-services">
        Dirección Creativa, Producción General, Producción Técnica, Museografía, Diseño Sonoro, Diseño de Espacios, Relatos, Editorial, Comunicaciones, Gestión de Prensa, Diseño, Render, Soporte, Fabricación, Fotografía y Audiovisual, Tramitación de Permisos y +
      </p>
      <img src="/img/logo.png" alt="MAPA MAPA" class="hero__logo" width="1080" height="auto" fetchpriority="high" decoding="async" />
    </div>
    <p class="hero__office reveal reveal-delay-2">Office</p>

    <div class="hero__collage" id="collage">
      <div class="collage__item collage__item--main" data-collage-item data-speed="0.15">
        <img src="/img/red.jpg" alt="MAPA MAPA — producción" width="400" height="400" decoding="async" />
      </div>
      <div class="collage__item collage__item--a" data-collage-item data-speed="0.3">
        <img src="/img/box.jpg" alt="" width="300" height="300" loading="lazy" decoding="async" />
      </div>
      <div class="collage__item collage__item--b" data-collage-item data-speed="0.45">
        <img src="/img/proy.jpg" alt="" width="300" height="300" loading="lazy" decoding="async" />
      </div>
      <div class="collage__item collage__item--c" data-collage-item data-speed="0.25">
        <img src="/img/bw.jpg" alt="" width="300" height="300" loading="lazy" decoding="async" />
      </div>
    </div>
  </section>

  <!-- Project list -->
  <section class="project-list">
    <div class="project-list__inner container">
      {projects.map((project, i) => (
        <div class={`project-cube reveal reveal-delay-${(i % 4) + 1}`}>
          <a href={project.url} class="project-row">
            <span class="project-row__num">{String(i + 1).padStart(2, '0')}</span>
            <span class="project-row__name">{project.name}</span>
            <span class="project-row__desc">{project.description}</span>
            <span class="project-row__cta">VER</span>
          </a>
          <a href={project.url} class="project-row project-row--back">
            <span class="project-row__num">{String(i + 1).padStart(2, '0')}</span>
            <span class="project-row__name">{project.name}</span>
            <span class="project-row__desc">{project.description}</span>
            <span class="project-row__cta">VER</span>
          </a>
        </div>
      ))}
    </div>
  </section>

  <!-- Producción & experiencias -->
  <section class="pye">
    <div class="pye__inner container">
      <h2 class="pye__title reveal">{pye.title}</h2>
      <div class="pye__grid">
        {pye.items.map((item, i) => (
          item.url ? (
            <a href={item.url} class={`pye__card reveal reveal-delay-${(i % 4) + 1}`}>
              <div class="pye__card-img">
                <img src={item.image} alt={item.title} width="600" height="600" loading="lazy" decoding="async" />
              </div>
              <h3 class="pye__card-title">{item.title}</h3>
              {item.subtitle && <p class="pye__card-subtitle">{item.subtitle}</p>}
            </a>
          ) : (
            <article class={`pye__card reveal reveal-delay-${(i % 4) + 1}`}>
              <div class="pye__card-img">
                <img src={item.image} alt={item.title} width="600" height="600" loading="lazy" decoding="async" />
              </div>
              <h3 class="pye__card-title">{item.title}</h3>
              {item.subtitle && <p class="pye__card-subtitle">{item.subtitle}</p>}
            </article>
          )
        ))}
      </div>
    </div>
  </section>

  <!-- Servicios Carousel -->
  <section class="carousel reveal" id="services-carousel" aria-label="Servicios">
    <button class="carousel__arrow carousel__arrow--prev" aria-label="Anterior" data-carousel-prev>&#8592;</button>
    <button class="carousel__arrow carousel__arrow--next" aria-label="Siguiente" data-carousel-next>&#8594;</button>
    <div class="carousel__track">
      {services.map((item) => (
        <a href={item.url ?? '#'} class="carousel__slide" data-slide>
          <img
            src={item.hero ?? item.image}
            alt={item.title}
            width="1600"
            height="900"
            loading="lazy"
            decoding="async"
          />
          <span class="carousel__label">{item.title}</span>
        </a>
      ))}
    </div>
  </section>
</BaseLayout>

<style>
  /* ===== HERO ===== */
  .hero {
    position: relative;
    min-height: 70dvh;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    padding: 0 var(--gutter) var(--space-xl);
    overflow: hidden;
  }

  .hero__services {
    position: absolute;
    top: 150px;
    left: 50%;
    transform: translateX(-50%);
    z-index: 1;
    width: 100%;
    max-width: 800px;
    font-size: var(--text-2xl);
    font-weight: 900;
    text-transform: uppercase;
    color: var(--color-hover);
    text-align: left;
    line-height: 1.0;
    letter-spacing: -0.01em;
    opacity: 0;
    cursor: pointer;
    transition: opacity 1s ease-in-out;
  }

  .hero__services--visible {
    opacity: 1;
  }

  .hero__services:hover {
    cursor: none;
  }

  .hero__logo-wrap {
    position: relative;
    z-index: var(--z-above);
    width: 100%;
    max-width: 1080px;
    margin-top: -20px;
    margin-bottom: clamp(-40px, -8vw, -100px);
    overflow: hidden;
  }

  .hero__logo {
    width: 100%;
    height: auto;
    filter: drop-shadow(0 0 40px rgba(255, 255, 255, 0.04));
  }

  .hero__office {
    position: relative;
    z-index: var(--z-above);
    font-family: var(--font-display);
    font-size: var(--text-3xl);
    font-weight: 900;
    letter-spacing: 0.02em;
    text-align: center;
    margin-top: clamp(-10px, -2vw, -30px);
  }

  /* Collage area below Office */
  .hero__collage {
    position: relative;
    width: 100%;
    max-width: var(--max-width);
    min-height: clamp(280px, 35vw, 500px);
    margin-top: var(--space-lg);
    padding-inline: var(--gutter);
  }

  .collage__item {
    position: absolute;
    overflow: hidden;
    will-change: transform;
    cursor: grab;
    touch-action: none;
  }

  .collage__item:active {
    cursor: grabbing;
  }

  .collage__item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    pointer-events: none;
    -webkit-user-drag: none;
    user-select: none;
  }

  .collage__item--main {
    width: clamp(200px, 26vw, 360px);
    aspect-ratio: 1;
    left: 0;
    top: 0;
    z-index: 2;
  }

  .collage__item--a {
    width: clamp(120px, 16vw, 220px);
    aspect-ratio: 1;
    right: 8%;
    top: 5%;
    z-index: 3;
  }

  .collage__item--b {
    width: clamp(100px, 13vw, 180px);
    aspect-ratio: 1;
    left: 32%;
    bottom: 0;
    z-index: 1;
  }

  .collage__item--c {
    width: clamp(110px, 14vw, 200px);
    aspect-ratio: 1;
    right: 28%;
    top: 0;
    z-index: 2;
  }

  /* ===== PROJECT LIST ===== */
  .project-list {
    padding: 20px 0;
  }

  .project-list__inner {
    max-width: 1000px;
  }

  .project-cube {
    perspective: 800px;
    height: 2.2em;
    position: relative;
  }

  .project-cube:hover .project-row {
    transform: rotateX(90deg);
  }

  .project-cube:hover .project-row--back {
    transform: rotateX(0deg);
    visibility: visible;
  }

  .project-row {
    display: grid;
    grid-template-columns: 50px 1fr 2fr auto;
    gap: var(--space-xs);
    align-items: baseline;
    padding: 0.2em 0.4em;
    text-decoration: none;
    height: 100%;
    backface-visibility: hidden;
    transition: transform 0.5s cubic-bezier(0.16, 1, 0.3, 1);
    transform-origin: center bottom;
    transform: rotateX(0deg);
  }

  .project-row--back {
    position: absolute;
    inset: 0;
    background-color: var(--color-accent);
    color: #000;
    transform-origin: center top;
    transform: rotateX(-90deg);
    visibility: hidden;
  }

  .project-cube:hover .project-row--back {
    visibility: visible;
  }

  .project-row--back span {
    color: #000;
  }

  .project-row--back:hover {
    color: #000;
  }

  .project-row span {
    font-size: var(--text-lg);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  .project-row__num {
    font-variant-numeric: tabular-nums;
  }

  .project-row__desc {
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .project-row__cta {
    transition: color var(--duration-fast);
  }

  @media (max-width: 768px) {
    .hero {
      min-height: auto;
      padding-top: 70px;
      padding-bottom: var(--space-md);
    }

    .hero__logo-wrap {
      margin-top: 0;
      margin-bottom: 0;
    }

    .hero__services {
      top: 0;
      left: 0;
      transform: none;
      max-width: 100%;
      font-size: var(--text-xl);
      padding: var(--space-xs);
    }

    .hero__office {
      margin-top: 0;
    }

    .hero__collage {
      min-height: 180px;
      margin-top: var(--space-sm);
    }

    .collage__item--main {
      width: 55%;
      left: 0;
    }

    .collage__item--a {
      width: 35%;
      right: 0;
      top: 10%;
    }

    .collage__item--b {
      width: 28%;
      left: 40%;
      bottom: 0;
    }

    .collage__item--c {
      width: 30%;
      right: 5%;
      top: auto;
      bottom: 5%;
    }

    .project-cube:hover .project-row {
      transform: rotateX(0deg);
    }

    .project-cube:hover .project-row--back {
      transform: rotateX(-90deg);
      visibility: hidden;
    }

    .project-row {
      grid-template-columns: 40px 1fr auto;
    }

    .project-row__desc {
      display: none;
    }
  }

  /* ===== PRODUCCIÓN & EXPERIENCIAS ===== */
  .pye {
    padding: 20px 0 var(--space-sm);
    border-top: 1px solid var(--color-border);
    border-bottom: 1px solid var(--color-border);
  }

  .pye__inner {
    max-width: none;
  }

  .pye__title {
    font-size: var(--text-2xl);
    font-weight: 900;
    text-align: center;
    margin-bottom: 20px;
  }

  .pye__grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-xs);
  }

  .pye__card-img {
    width: 100%;
    aspect-ratio: 5 / 4;
    overflow: hidden;
  }

  .pye__card-img img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .pye__card-title {
    font-size: var(--text-base);
    text-transform: uppercase;
    letter-spacing: 0.03em;
    line-height: 1.05;
    margin-top: var(--space-xs);
  }

  .pye__card-subtitle {
    font-size: var(--text-base);
  }

  @media (max-width: 768px) {
    .pye__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .pye__grid {
      grid-template-columns: 1fr;
    }
  }

  /* ===== SERVICIOS CAROUSEL ===== */
  .carousel {
    position: relative;
    width: 100%;
    overflow: hidden;
    padding: var(--space-md) 0 var(--space-lg);
  }

  .carousel__arrow {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    width: 48px;
    height: 48px;
    border: none;
    background: var(--color-accent, #df9de5);
    color: var(--color-bg, #000);
    font-size: 1.3rem;
    font-weight: 900;
    cursor: pointer;
    opacity: 0.85;
    transition: opacity 0.2s;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .carousel__arrow:hover {
    opacity: 1;
  }

  .carousel__arrow--prev { left: clamp(0.5rem, 2vw, 1.5rem); }
  .carousel__arrow--next { right: clamp(0.5rem, 2vw, 1.5rem); }

  .carousel__track {
    display: flex;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    gap: 0.75rem;
    cursor: grab;
    user-select: none;
    touch-action: pan-y;
  }

  .carousel__track.is-dragging {
    cursor: grabbing;
    scroll-snap-type: none;
  }

  .carousel__track::-webkit-scrollbar {
    display: none;
  }

  .carousel__slide {
    position: relative;
    flex: 0 0 calc((100% - 1.5rem) / 3);
    aspect-ratio: 16 / 9;
    overflow: hidden;
    text-decoration: none;
    display: block;
  }

  .carousel__slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
    pointer-events: none;
    -webkit-user-drag: none;
  }

  .carousel__label {
    position: absolute;
    bottom: clamp(0.8rem, 2vw, 1.5rem);
    left: clamp(0.6rem, 2vw, 1.2rem);
    font-size: clamp(0.9rem, 0.6rem + 1.2vw, 1.6rem);
    font-weight: 900;
    text-transform: uppercase;
    color: var(--color-bg, #0a0a0a);
    background: var(--color-accent, #df9de5);
    line-height: 1.4;
    letter-spacing: -0.01em;
    pointer-events: none;
    padding: 0.1em 0.3em;
    box-decoration-break: clone;
    -webkit-box-decoration-break: clone;
  }

  @media (max-width: 768px) {
    .carousel__track {
      gap: 0;
    }

    .carousel__slide {
      flex: 0 0 100%;
      aspect-ratio: 4 / 3;
    }

    .carousel__label {
      font-size: clamp(1.4rem, 1rem + 2vw, 2.5rem);
      bottom: clamp(1rem, 3vw, 2rem);
      left: clamp(0.8rem, 3vw, 2rem);
    }

    .carousel__arrow {
      width: 36px;
      height: 36px;
      font-size: 1rem;
    }
  }
</style>

<script>
  import { gsap, ScrollTrigger } from '@/scripts/gsap';

  function initServicesToggle() {
    const el = document.getElementById('hero-services');
    if (!el) return;

    let cursor: HTMLElement | null = document.getElementById('services-cursor');
    if (!cursor) {
      cursor = document.createElement('div');
      cursor.id = 'services-cursor';
      cursor.style.cssText = 'position:fixed;top:0;left:0;width:50px;height:50px;background:#df9de5;pointer-events:none;z-index:9999;opacity:0;will-change:transform;';
      document.body.appendChild(cursor);
    }

    el.addEventListener('click', () => {
      el.classList.toggle('hero__services--visible');
    });

    el.addEventListener('mouseenter', (e: MouseEvent) => {
      gsap.set(cursor!, { x: e.clientX - 25, y: e.clientY - 25 });
      gsap.to(cursor!, { opacity: 1, duration: 0.15 });
    });

    el.addEventListener('mouseleave', () => {
      gsap.to(cursor!, { opacity: 0, duration: 0.15 });
    });

    el.addEventListener('mousemove', (e: MouseEvent) => {
      gsap.set(cursor!, { x: e.clientX - 25, y: e.clientY - 25 });
    });
  }
  initServicesToggle();
  document.addEventListener('astro:after-swap', initServicesToggle);

  function initMobileProjectCubes() {
    if (!window.matchMedia('(pointer: coarse)').matches) return;

    document.querySelectorAll<HTMLElement>('.project-cube').forEach((cube) => {
      const front = cube.querySelector<HTMLElement>('.project-row:not(.project-row--back)');
      const back = cube.querySelector<HTMLElement>('.project-row--back');
      const link = cube.querySelector<HTMLAnchorElement>('a');
      if (!front || !back || !link) return;

      cube.addEventListener('click', (e) => {
        e.preventDefault();
        const url = link.href;

        gsap.to(front, { rotateX: 90, duration: 0.35, ease: 'power2.in' });
        gsap.set(back, { visibility: 'visible' });
        gsap.fromTo(back, { rotateX: -90 }, {
          rotateX: 0,
          duration: 0.35,
          ease: 'power2.out',
          onComplete: () => { window.location.href = url; },
        });
      });
    });
  }

  initMobileProjectCubes();
  document.addEventListener('astro:after-swap', initMobileProjectCubes);

  const COLLAGE_ST_ID = 'collage';
  let collageMouseCleanup: (() => void) | null = null;

  function killCollageTriggers() {
    ScrollTrigger.getAll()
      .filter((t) => (t.vars as any).id === COLLAGE_ST_ID)
      .forEach((t) => t.kill());
  }

  function initCollage() {
    killCollageTriggers();
    if (collageMouseCleanup) { collageMouseCleanup(); collageMouseCleanup = null; }

    const collage = document.getElementById('collage');
    const items = document.querySelectorAll<HTMLElement>('[data-collage-item]');
    if (!collage || !items.length) return;

    gsap.set(items, { autoAlpha: 0, y: 40, scale: 0.92 });

    gsap.to(items, {
      autoAlpha: 1,
      y: 0,
      scale: 1,
      duration: 0.9,
      ease: 'expo.out',
      stagger: 0.12,
      scrollTrigger: {
        id: COLLAGE_ST_ID,
        trigger: collage,
        start: 'top bottom-=80px',
        once: true,
      },
    });

    if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) return;

    items.forEach((item) => {
      const speed = parseFloat(item.dataset.speed || '0.2');
      gsap.to(item, {
        yPercent: -speed * 100,
        ease: 'none',
        scrollTrigger: {
          id: COLLAGE_ST_ID,
          trigger: collage,
          start: 'top bottom',
          end: 'bottom top',
          scrub: true,
        },
      });
    });

    const FRICTION = 0.92;
    const COLLISION_BOUNCE = 0.5;

    interface Ball {
      el: HTMLElement;
      ox: number;
      oy: number;
      vx: number;
      vy: number;
      vr: number;
      r: number;
      dragging: boolean;
    }

    const balls: Ball[] = Array.from(items).map((el) => ({
      el,
      ox: 0,
      oy: 0,
      vx: 0,
      vy: 0,
      vr: 0,
      r: el.offsetWidth / 2,
      dragging: false,
    }));

    function getCenter(b: Ball) {
      const rect = b.el.getBoundingClientRect();
      return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 };
    }

    let ticking = false;

    function step() {
      let moving = false;

      for (let i = 0; i < balls.length; i++) {
        for (let j = i + 1; j < balls.length; j++) {
          const a = balls[i];
          const b = balls[j];
          const ac = getCenter(a);
          const bc = getCenter(b);
          const dx = bc.x - ac.x;
          const dy = bc.y - ac.y;
          const dist = Math.sqrt(dx * dx + dy * dy) || 1;
          const minDist = a.r + b.r;

          if (dist < minDist) {
            const nx = dx / dist;
            const ny = dy / dist;
            const relVx = a.vx - b.vx;
            const relVy = a.vy - b.vy;
            const impact = relVx * nx + relVy * ny;

            if (impact > 0) {
              const impulse = impact * COLLISION_BOUNCE;
              if (!a.dragging) { a.vx -= impulse * nx; a.vy -= impulse * ny; a.vr += impulse * 0.8; }
              if (!b.dragging) { b.vx += impulse * nx; b.vy += impulse * ny; b.vr -= impulse * 0.8; }
            }

            const overlap = (minDist - dist) / 2 + 0.5;
            if (!a.dragging) { a.ox -= nx * overlap; a.oy -= ny * overlap; }
            if (!b.dragging) { b.ox += nx * overlap; b.oy += ny * overlap; }
          }
        }
      }

      for (const b of balls) {
        if (!b.dragging) {
          b.vx *= FRICTION;
          b.vy *= FRICTION;
          b.vr *= FRICTION;
          b.ox += b.vx;
          b.oy += b.vy;
        }

        const speed = Math.abs(b.vx) + Math.abs(b.vy) + Math.abs(b.vr);
        if (speed > 0.1 || b.dragging) moving = true;

        gsap.set(b.el, { x: b.ox, y: b.oy, rotation: b.vr * 3 });
      }

      if (moving) {
        requestAnimationFrame(step);
      } else {
        ticking = false;
      }
    }

    function kick() {
      if (!ticking) {
        ticking = true;
        requestAnimationFrame(step);
      }
    }

    function attractAll(px: number, py: number, force: number) {
      for (const b of balls) {
        if (b.dragging) continue;
        const center = getCenter(b);
        const dx = px - center.x;
        const dy = py - center.y;
        const dist = Math.sqrt(dx * dx + dy * dy) || 1;
        b.vx += (dx / dist) * force;
        b.vy += (dy / dist) * force;
        b.vr += (dx / dist) * 2;
      }
      kick();
    }

    const isMobile = window.matchMedia('(pointer: coarse)').matches;

    if (isMobile) {
      items.forEach((item, i) => {
        let dragBall: Ball | null = null;
        let lastX = 0;
        let lastY = 0;

        item.addEventListener('touchstart', (e: TouchEvent) => {
          const touch = e.touches[0];
          dragBall = balls[i];
          dragBall.dragging = true;
          dragBall.vx = 0;
          dragBall.vy = 0;
          lastX = touch.clientX;
          lastY = touch.clientY;
          kick();
          e.preventDefault();
        }, { passive: false });

        item.addEventListener('touchmove', (e: TouchEvent) => {
          if (!dragBall) return;
          const touch = e.touches[0];
          const dx = touch.clientX - lastX;
          const dy = touch.clientY - lastY;
          dragBall.ox += dx;
          dragBall.oy += dy;
          dragBall.vx = dx * 0.6;
          dragBall.vy = dy * 0.6;
          lastX = touch.clientX;
          lastY = touch.clientY;
          e.preventDefault();
        }, { passive: false });

        function endDrag() {
          if (dragBall) {
            dragBall.dragging = false;
            dragBall = null;
          }
        }

        item.addEventListener('touchend', endDrag);
        item.addEventListener('touchcancel', endDrag);
      });

      collage.addEventListener('touchstart', (e: TouchEvent) => {
        const isDraggingItem = (e.target as HTMLElement).closest('[data-collage-item]');
        if (isDraggingItem) return;
        const touch = e.touches[0];
        attractAll(touch.clientX, touch.clientY, 10);
      }, { passive: true });
    } else {
      const HIT_FORCE = 8;
      let activeDrag: { ball: Ball; idx: number } | null = null;
      let lastMX = 0;
      let lastMY = 0;
      let didDrag = false;

      items.forEach((item, i) => {
        item.addEventListener('mousedown', (e: MouseEvent) => {
          const b = balls[i];
          activeDrag = { ball: b, idx: i };
          b.dragging = true;
          b.vx = 0;
          b.vy = 0;
          lastMX = e.clientX;
          lastMY = e.clientY;
          didDrag = false;
          item.style.zIndex = '10';
          document.body.style.userSelect = 'none';
          kick();
          e.preventDefault();
        });

        item.addEventListener('mouseenter', (e: MouseEvent) => {
          if (activeDrag) return;
          const b = balls[i];
          const rect = item.getBoundingClientRect();
          const cx = rect.left + rect.width / 2;
          const cy = rect.top + rect.height / 2;
          const dx = cx - e.clientX;
          const dy = cy - e.clientY;
          const dist = Math.sqrt(dx * dx + dy * dy) || 1;

          b.vx += (dx / dist) * HIT_FORCE;
          b.vy += (dy / dist) * HIT_FORCE;
          b.vr += (dx / dist) * 3;

          kick();
        });
      });

      const onMouseMove = (e: MouseEvent) => {
        if (!activeDrag) return;
        const dx = e.clientX - lastMX;
        const dy = e.clientY - lastMY;
        activeDrag.ball.ox += dx;
        activeDrag.ball.oy += dy;
        activeDrag.ball.vx = dx * 0.6;
        activeDrag.ball.vy = dy * 0.6;
        lastMX = e.clientX;
        lastMY = e.clientY;
        if (Math.abs(dx) > 2 || Math.abs(dy) > 2) didDrag = true;
      };

      const onMouseUp = () => {
        if (!activeDrag) return;
        activeDrag.ball.dragging = false;
        items[activeDrag.idx].style.zIndex = '';
        activeDrag = null;
        document.body.style.userSelect = '';
      };

      window.addEventListener('mousemove', onMouseMove);
      window.addEventListener('mouseup', onMouseUp);

      if (collageMouseCleanup) collageMouseCleanup();
      collageMouseCleanup = () => {
        window.removeEventListener('mousemove', onMouseMove);
        window.removeEventListener('mouseup', onMouseUp);
      };

      collage.addEventListener('click', (e: MouseEvent) => {
        if (didDrag) { didDrag = false; return; }
        attractAll(e.clientX, e.clientY, 12);
      });
    }
  }

  initCollage();
  document.addEventListener('astro:after-swap', () => {
    requestAnimationFrame(initCollage);
  });

  let carouselCleanup: (() => void) | null = null;

  function initCarousel() {
    if (carouselCleanup) { carouselCleanup(); carouselCleanup = null; }

    const track = document.querySelector<HTMLElement>('.carousel__track');
    if (!track) return;

    track.querySelectorAll('[data-clone]').forEach((el) => el.remove());

    const originals = Array.from(track.querySelectorAll<HTMLElement>('[data-slide]'));
    const count = originals.length;
    if (!count) return;

    originals.forEach((slide) => {
      const clone = slide.cloneNode(true) as HTMLElement;
      clone.removeAttribute('data-slide');
      clone.setAttribute('data-clone', '');
      track.appendChild(clone);
    });

    const allSlides = Array.from(track.querySelectorAll<HTMLElement>('.carousel__slide'));
    let current = 0;
    let timer: ReturnType<typeof setInterval> | null = null;
    let isResetting = false;
    let scrollTween: gsap.core.Tween | null = null;

    function getSlideStep(): number {
      if (allSlides.length < 2) return allSlides[0]?.offsetWidth ?? 0;
      return allSlides[1].offsetLeft - allSlides[0].offsetLeft;
    }

    function smoothScrollTo(target: number): Promise<void> {
      return new Promise((resolve) => {
        if (scrollTween) scrollTween.kill();
        const start = track!.scrollLeft;
        if (Math.abs(target - start) < 1) { resolve(); return; }

        scrollTween = gsap.to(track!, {
          scrollLeft: target,
          duration: 1.2,
          ease: 'power3.inOut',
          onComplete: () => {
            scrollTween = null;
            resolve();
          },
        });
      });
    }

    function silentReset(to: number) {
      isResetting = true;
      track!.scrollLeft = getSlideStep() * to;
      current = to;
      requestAnimationFrame(() => {
        isResetting = false;
      });
    }

    function advance() {
      if (scrollTween) return;
      if (current >= count) {
        silentReset(current - count);
        return;
      }
      const next = current + 1;
      current = next;
      const target = getSlideStep() * current;
      if (next >= count) {
        smoothScrollTo(target).then(() => silentReset(next - count));
      } else {
        smoothScrollTo(target);
      }
    }

    function retreat() {
      if (scrollTween) return;
      if (current <= 0) {
        silentReset(count);
      }
      current = current - 1;
      const target = getSlideStep() * current;
      smoothScrollTo(target);
    }

    function startAuto() {
      stopAuto();
      timer = setInterval(advance, 4500);
    }

    function stopAuto() {
      if (timer) { clearInterval(timer); timer = null; }
    }

    const prevBtn = document.querySelector<HTMLElement>('[data-carousel-prev]');
    const nextBtn = document.querySelector<HTMLElement>('[data-carousel-next]');
    prevBtn?.addEventListener('click', () => { stopAuto(); retreat(); startAuto(); });
    nextBtn?.addEventListener('click', () => { stopAuto(); advance(); startAuto(); });

    let scrollTimeout: ReturnType<typeof setTimeout>;
    track.addEventListener('scroll', () => {
      if (isResetting || scrollTween) return;
      clearTimeout(scrollTimeout);
      scrollTimeout = setTimeout(() => {
        const step = getSlideStep();
        if (step > 0) {
          const idx = Math.round(track!.scrollLeft / step);
          if (idx !== current) {
            current = idx;
            if (current >= count) {
              silentReset(current - count);
            }
          }
        }
      }, 100);
    }, { passive: true });

    /* --- Drag-to-scroll (mouse + touch) --- */
    let isDragging = false;
    let dragStartX = 0;
    let dragScrollLeft = 0;
    let dragVelocity = 0;
    let lastDragX = 0;
    let lastDragTime = 0;

    let didDragCarousel = false;
    let pendingPointerId: number | null = null;
    const DRAG_THRESHOLD = 8;

    function onPointerDown(e: PointerEvent) {
      if (e.button !== 0) return;
      isDragging = false;
      didDragCarousel = false;
      pendingPointerId = e.pointerId;
      dragStartX = e.clientX;
      dragScrollLeft = track!.scrollLeft;
      dragVelocity = 0;
      lastDragX = e.clientX;
      lastDragTime = Date.now();
      stopAuto();
      if (scrollTween) { scrollTween.kill(); scrollTween = null; }
    }

    function onPointerMove(e: PointerEvent) {
      if (pendingPointerId === null && !isDragging) return;
      const dx = e.clientX - dragStartX;

      if (!isDragging && Math.abs(dx) > DRAG_THRESHOLD) {
        isDragging = true;
        didDragCarousel = true;
        track!.classList.add('is-dragging');
        track!.setPointerCapture(e.pointerId);
        pendingPointerId = null;
      }

      if (!isDragging) return;
      track!.scrollLeft = dragScrollLeft - dx;

      const now = Date.now();
      const dt = now - lastDragTime || 1;
      dragVelocity = (e.clientX - lastDragX) / dt;
      lastDragX = e.clientX;
      lastDragTime = now;
    }

    function onPointerUp(e: PointerEvent) {
      pendingPointerId = null;
      if (!isDragging) { startAuto(); return; }
      isDragging = false;
      track!.classList.remove('is-dragging');
      track!.releasePointerCapture(e.pointerId);

      const momentum = -dragVelocity * 300;
      if (Math.abs(momentum) > 10) {
        gsap.to(track!, {
          scrollLeft: track!.scrollLeft + momentum,
          duration: 0.6,
          ease: 'power2.out',
          onComplete: () => { snapToNearest(); startAuto(); },
        });
      } else {
        snapToNearest();
        startAuto();
      }
    }

    function snapToNearest() {
      const step = getSlideStep();
      if (step <= 0) return;
      const idx = Math.round(track!.scrollLeft / step);
      current = idx;
      if (current >= count) {
        silentReset(current - count);
      }
      gsap.to(track!, { scrollLeft: step * current, duration: 0.35, ease: 'power2.out' });
    }

    track.addEventListener('pointerdown', onPointerDown);
    track.addEventListener('pointermove', onPointerMove);
    track.addEventListener('pointerup', onPointerUp);
    track.addEventListener('pointercancel', onPointerUp);

    track.addEventListener('click', (e: MouseEvent) => {
      if (didDragCarousel) {
        e.preventDefault();
        e.stopPropagation();
      }
    }, { capture: true });

    track.addEventListener('touchstart', stopAuto, { passive: true });
    track.addEventListener('touchend', () => startAuto());

    let resizeTimeout: ReturnType<typeof setTimeout>;
    function onResize() {
      clearTimeout(resizeTimeout);
      resizeTimeout = setTimeout(() => {
        if (current >= count) silentReset(current - count);
        track!.scrollTo({ left: getSlideStep() * current, behavior: 'instant' as ScrollBehavior });
      }, 150);
    }
    window.addEventListener('resize', onResize);

    startAuto();

    carouselCleanup = () => {
      stopAuto();
      if (scrollTween) scrollTween.kill();
      window.removeEventListener('resize', onResize);
      track.removeEventListener('pointerdown', onPointerDown);
      track.removeEventListener('pointermove', onPointerMove);
      track.removeEventListener('pointerup', onPointerUp);
      track.removeEventListener('pointercancel', onPointerUp);
      track.querySelectorAll('[data-clone]').forEach((el) => el.remove());
    };
  }

  initCarousel();
  document.addEventListener('astro:after-swap', initCarousel);
</script>
